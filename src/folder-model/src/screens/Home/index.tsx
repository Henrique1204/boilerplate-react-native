import React from 'react';
import { Text } from 'react-native';

import { MainContainer } from '@components/index';

const Home: React.FC = () => {
	return (
		<MainContainer>
			<Text>Olá Mundo</Text>
		</MainContainer>
	);
};

export default Home;
